Бригада 6
1. Напишите запрос, который выводит все строки из таблицы Покупателей, для которых рейтинг покупателя равен 100.
   SELECT * FROM cust WHERE rating = 100

2. Напишите запрос, который выводит суммы заказов, дату и номера продавцов для всех строк из таблицы Заказов.
   SELECT amt,odate,snum FROM ord 

3. Напишите запрос, который выводит номер и имя покупателя, место расположения которого - Рим.
   SELECT cnum, cname FROM cust where(city = 'Rome')

4. Напишите запрос, который выводит значения номеров продавцов из таблицы Заказов без каких бы то ни было повторений.
   SELECT DISTINCT  snum FROM ord 

5. Напишите запрос, который может выдать вам поля sname и city для всех продавцов не в San Jose с комиссионными строго больше 0.11
   SELECT sname, city FROM sal where (city != 'San Jose' and  comm> .11)

6. Напишите запрос к таблице Покупателей, чей вывод может включить всех покупателей с оценкой строго больше 200, если они находятся в San Jose
   SELECT * from cust where (rating>200 and city = 'San Jose')

7. Запросите двумя способами все заказы покупателей с номерами 2002 и 2008.
   select * from ord where cnum = 2008 or cnum = 2002
   select * from ord where (cnum >2001 and cnum <2003) or cnum >2007